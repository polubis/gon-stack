# {id} - {summary}

optional keys: `models`, `inputTokens`, `outTokens`, `cost`, `workTimeInMinutes` — include only if known from the conversation, skip otherwise

```json
{
  "status": "done" | "failed" | "done-with-clarification"
}
```

{descriptionOfChanges}

{reason}

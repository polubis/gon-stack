---
export const prerender = true;

import MainLayout from '@/core/layouts/main.astro';
import { Main as Module } from '@/modules/privacy-policy/presentation/main';
import { copy } from '@/modules/privacy-policy/presentation/copy';

const domain = Astro.locals.siteDomain;
const imageUrl = `${domain}/og.webp`;
---

<MainLayout
  author={copy.meta.author}
  siteName={copy.meta.siteName}
  url="/polityka-prywatnosci/"
  imageAlt={copy.meta.imageAlt}
  domain={domain}
  imageUrl={imageUrl}
  ogImageWidth={1200}
  ogImageHeight={630}
  keywords={copy.meta.keywords}
  title={copy.meta.title}
  description={copy.meta.description}
>
  <Module />
</MainLayout>

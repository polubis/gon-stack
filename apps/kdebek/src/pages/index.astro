---
export const prerender = true;

import MainLayout from '@/core/layouts/main.astro';
import { Main as Module, copy, resolveHomeImages } from '@/modules/home';

const images = await resolveHomeImages();
---

<MainLayout title={copy.meta.title} description={copy.meta.description}>
  <Module client:load images={images} />
</MainLayout>
